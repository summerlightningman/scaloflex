connection = {
  "host" = "localhost",
  "port" = 8080
}
database = {
  url = "jdbc:postgresql://localhost:5432/postgres"
  driver = org.postgresql.driver
  user = "postgres"
  password = "1337"
  numThreads = 10
  keepAliveConnection = true
}